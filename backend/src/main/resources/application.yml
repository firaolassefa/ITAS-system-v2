spring:
  application:
    name: itas-backend
  
  datasource:
    url: jdbc:postgresql://ep-hidden-king-aiwufvo3-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_sCb7FRYEPhL0
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true
  
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

server:
  port: 8080
  servlet:
    context-path: /api

app:
  jwt:
    secret: ${JWT_SECRET:itas-tax-education-system-secret-key-2024-change-this-in-production-use-env-variable}
    expiration: ${JWT_EXPIRATION:3600000} # 1 hour in milliseconds (changed from 24h for security)
  file:
    upload-dir: ./uploads
    max-size: 104857600 # 100MB
    allowed-extensions: pdf,mp4,mp3,jpg,jpeg,png,doc,docx,xls,xlsx
  cors:
    allowed-origins: http://localhost:3000,http://localhost:8080
    allowed-methods: GET,POST,PUT,DELETE,OPTIONS
    allowed-headers: "*"

logging:
  level:
    com.itas: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE